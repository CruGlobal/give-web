extend type Query {
  cart: Cart
}

type Cart {
  id: String!
  items: [CartItem!]!
  cartTotal: Float!
  cartTotalDisplay: String!
}

type CartItem {
  amount: Float!
  code: String!
  displayName: String!
  orgId: String
  price: String!
  uri: String!
}
